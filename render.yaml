services:
  - type: web
    name: student-record-app
    env: python
    buildCommand: "CARGO_HOME=/tmp/cargo pip install --no-cache-dir -r requirements.txt"
    startCommand: "gunicorn app:app"
    plan: free
